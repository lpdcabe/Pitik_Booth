import { frames } from '../../utils/frames';
import { getRemoteTemplates } from '../../utils/remoteLayouts';
export default function RoomSettings({value,onChange,participantCount,disabled=false,create=false}){
 const count=participantCount||Number(value.maxParticipants)||2;
 const templates=getRemoteTemplates(count);
 const change=(key,v)=>onChange({...value,[key]:v});
 return <fieldset className="room-settings" disabled={disabled}><div className="field-grid">{create&&<label>Maximum participants<select value={value.maxParticipants} onChange={e=>onChange({...value,maxParticipants:Number(e.target.value),layout:getRemoteTemplates(Number(e.target.value))[0].id})}>{[2,3,4].map(n=><option key={n} value={n}>{n} people</option>)}</select></label>}<label>Photos per person<select value={value.photoCount} onChange={e=>change('photoCount',Number(e.target.value))}>{[1,2,3,4,6,8,9].map(n=><option key={n} value={n}>{n} {n===1?'photo':'photos'}</option>)}</select></label><label>Countdown<select value={value.countdownSeconds} onChange={e=>change('countdownSeconds',Number(e.target.value))}>{[3,5,10].map(n=><option key={n} value={n}>{n} seconds</option>)}</select></label><label>Frame<select value={value.frame} onChange={e=>change('frame',e.target.value)}>{frames.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select></label></div><label>Layout for {count} people<select value={templates.some(t=>t.id===value.layout)?value.layout:templates[0].id} onChange={e=>change('layout',e.target.value)}>{templates.map(t=><option value={t.id} key={t.id}>{t.name}</option>)}</select></label><div className="remote-template-grid">{templates.map(t=><button type="button" key={t.id} aria-label={`Choose ${t.name}`} aria-pressed={value.layout===t.id} className={value.layout===t.id?'selected':''} onClick={()=>change('layout',t.id)}><img src={t.thumbnail} alt=""/><span>{t.name}</span></button>)}</div><label className="inline-check"><input type="checkbox" checked={value.autoContinue} onChange={e=>change('autoContinue',e.target.checked)}/> Automatically start the next photo when everyone approves</label></fieldset>;
}
